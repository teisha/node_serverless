#serverless.yml

service: tutorial-express-application

provider:
  name: aws
  runtime: nodejs12.14.0
  stage: dev
  region: us-east-1


functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'